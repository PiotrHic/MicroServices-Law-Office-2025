#Basic Config
spring.application.name=Lawyer-Service
server.port=8011

#Eureka Configuration
eureka.client.serviceUrl.defaultZone=http://127.0.0.1:8761/eureka

# Spring Data MongoDB configuration
spring.data.mongodb.uri=mongodb://localhost:27017/Lawyers
spring.data.mongodb.database=lawofficedb
spring.data.mongodb.auto-index-creation=true

# Optional: Logging for MongoDB queries
logging.level.org.springframework.data.mongodb.core.MongoTemplate=DEBUG
logging.level.org.mongodb.driver=INFO

#Open APi Config
# Include actuator endpoints (if you use Spring Boot Actuator)
springdoc.show-actuator=true

#Actuator + Prometheus
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always